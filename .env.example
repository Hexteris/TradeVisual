# .env.example
# Environment configuration for Trading Journal

# Database URL (local development uses SQLite, production uses Neon PostgreSQL)
# Local: DATABASE_URL=sqlite:///./trading_journal.db
# Neon: DATABASE_URL=postgresql+psycopg2://user:password@ep-xxx.neon.tech/database

DATABASE_URL=sqlite:///./trading_journal.db

# Streamlit settings
STREAMLIT_THEME=light

# Render deployment (if using Render)
# RENDER_EXTERNAL_URL=https://your-app.onrender.com
